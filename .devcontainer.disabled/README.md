# Devcontainer Disabled

This devcontainer has been disabled to prevent recovery mode errors.

The project works fine without it - just use:
- Node.js (already available in Codespaces)
- SQLite (no setup needed)

To re-enable, rename this folder back to `.devcontainer`


